/** @format */

import musicMenu from "../data/favorites-items";
import MenuTemplate from "./MenuTemplate";

const Favorites = () => {
  return <MenuTemplate menuItems={musicMenu} />;
};

export default Favorites;
